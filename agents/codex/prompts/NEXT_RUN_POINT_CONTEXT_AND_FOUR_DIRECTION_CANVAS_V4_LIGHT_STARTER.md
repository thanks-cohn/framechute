Read and execute `agents/codex/prompts/NEXT_RUN_POINT_CONTEXT_AND_FOUR_DIRECTION_CANVAS_V4.md` from the latest `main`. Treat it as the single authoritative prompt for this run and build on PR #51 rather than replacing its editor-context/menu foundations. LightComp is the web-first authoring/editor system; the portable file format is `WEBX` with extension `.webx`, implemented as a ZIP-like package of ordinary web primitives such as a versioned manifest, `index.html`, CSS, JavaScript when needed, semantic Markdown/CommonDocument source where practical, and local assets. Prioritize: fix the remaining PR #51 invariants; point-based right-click context without preselection; true four-direction canvas expansion with stable world coordinates and no left/top clamp; top-toolbar `New → WEBX / DOCX / PDF`; WEBX Save/Open round-trip and static web deployment readiness; then non-destructive `Convert → WEBX / DOCX / PDF` through the shared CommonDocument bridge. Design WEBX for interoperability with Markdown/Joplin-like editors, static-site generators, CMS/publishing tools, browsers, and ordinary web hosts through HTML/CSS/JS/assets/Markdown adapters without falsely claiming native `.webx` support. Preserve document-specific menus, Simple-mode restrictions, video-only sync capability, internal-drag ingest suppression, snapshot Save As, header/chrome idempotence, and preview/serialization correctness. Use up to 30 minutes, run the required tests/syntax/diff/package validation, and at the 30-minute mark **conclude active implementation and provide a handoff**.