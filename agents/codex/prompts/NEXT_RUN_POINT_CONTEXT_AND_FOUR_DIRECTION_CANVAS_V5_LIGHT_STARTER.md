Read and execute `agents/codex/prompts/NEXT_RUN_POINT_CONTEXT_AND_FOUR_DIRECTION_CANVAS_V5.md` from the latest `main`. Treat it as the single authoritative prompt for this run. Build on PR #51 and the V4 WEBX contract rather than replacing those foundations. First fix the broken context submenu geometry: when a user opens the right-click menu and hovers/activates `Open ›`, `Arrange ›`, `Show ›`, Font, Text Size, or any other nested item, the child submenu must appear immediately beside the trigger row with only a small gap; it must never appear halfway across or near the far-right edge of the viewport. Use one consistent viewport coordinate system, flip left near the right edge, and clamp vertically only as much as necessary. Then enforce distinct format-native right-click menus for PDF, DOCX, and WEBX; add canonical Undo/Redo with Ctrl/Cmd+Z and Redo support; bring PDF and DOCX to a credible minimal standard of workmanship; preserve point-based context, four-direction canvas growth, Quick Actions red ×/no Clear, Simple-mode restrictions, video-only sync, Snapshot Save As, WEBX Save/Open and static web deployment readiness, and `New / Convert → WEBX / DOCX / PDF`. Use up to 30 minutes, run full tests/syntax/diff/package validation, and at the 30-minute mark **conclude active implementation and provide a handoff**.