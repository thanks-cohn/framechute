Read and execute `agents/codex/prompts/NEXT_RUN_PR55_RECONCILE_AND_STABILIZE_DOCX_PDF_MENUS_V10.md` on branch `codex/implement-docx-editing-and-context-menu`. Treat it as the single authoritative prompt for this run. Before editing, read both `agents/codex/debug/PR55_STABILIZATION_MAP_V10.md` and `agents/codex/debug/IMAGE_DRAG_INTERCHANGE_V10.md`.

First reconcile PR #55 with the latest `main` without losing merged PR #54 drag/image-drop behavior. Then **retire the global full-workspace `Drop into FrameChute` overlay entirely while preserving actual drop/ingest capability**. It must be impossible over DOCX/PDF surfaces, impossible during any internal FrameChute image drag, and absent even for external OS/browser drags; local destination-scoped feedback is fine, but no replacement full-workspace overlay.

Make image drag ownership universal: DOCX embedded images must be movable/reorderable inside the same DOCX instead of spawning a workspace frame; PDF editable images must be movable inside PDF and draggable out to workspace/DOCX using real image bytes. Same-container drag means move; cross-container drag means non-destructive copy by default. Also fix detached submenu geometry, omit all timed-move/layer-timing commands when Advanced is OFF, restrict `Sync with...`/`Make independent` to real playable audio/video only, harden DOCX Find/Replace/numbering/style round-trip, make PDF Enter/newlines/tabs/structured whitespace behave like normal text editing, and add PDF-specific Settings with `Deleting Text Deletes Link [ON]` by default so linked-text deletion removes/restores the associated annotation in the same undo step.

Use the available runtime aggressively. At approximately the 30-minute reliability checkpoint, **conclude active implementation and provide a handoff** exactly as required by V10.