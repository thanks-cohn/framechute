Read and execute `agents/codex/prompts/NEXT_RUN_PR55_RECONCILE_AND_STABILIZE_DOCX_PDF_MENUS_V10.md` on branch `codex/implement-docx-editing-and-context-menu`. Treat it as the single authoritative prompt for this run. Before editing, read both `agents/codex/debug/PR55_STABILIZATION_MAP_V10.md` and `agents/codex/debug/IMAGE_DRAG_INTERCHANGE_V10.md`.

First reconcile PR #55 with the latest `main` without losing merged PR #54 drag/image-drop behavior. Then enforce the corrected drag UX: DOCX and PDF surfaces must never show the `Drop into FrameChute` overlay, and picking up any image that is already inside FrameChute must never show that overlay anywhere. Do NOT replace it with a new DOCX/PDF overlay. The user's immediate goal is direct manipulation: pick up an embedded DOCX image and move it up/down to a new document position; pick up a PDF editable image and reposition it inside the PDF. Same-container drag means move the same semantic image; it must not spawn a new workspace frame or duplicate itself.

Also make cross-container image interchange work from canonical image bytes, fix detached submenu geometry, omit all timed-move/layer-timing commands when Advanced is OFF, restrict `Sync with...`/`Make independent` to real playable audio/video only, harden DOCX Find/Replace/numbering/style round-trip, make PDF Enter/newlines/tabs/structured whitespace behave like normal text editing, and add PDF-specific Settings with `Deleting Text Deletes Link [ON]` by default so linked-text deletion removes/restores the associated annotation in the same undo step.

Use the available runtime aggressively. At approximately the 30-minute reliability checkpoint, **conclude active implementation and provide a handoff** exactly as required by V10.