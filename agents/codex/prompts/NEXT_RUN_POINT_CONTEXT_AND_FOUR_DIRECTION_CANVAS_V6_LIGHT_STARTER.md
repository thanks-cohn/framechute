Read and execute `agents/codex/prompts/NEXT_RUN_POINT_CONTEXT_AND_FOUR_DIRECTION_CANVAS_V6.md` from the latest `main`. Treat it as the single authoritative prompt for this run. Build on V5 rather than replacing its submenu, format-native menu, Undo/Redo, WEBX, and document-editing foundations. First restore `Show image only` and make framed image rotation look professionally contained; add persistent Quick Actions minimize/maximize state that survives object selection changes while keeping the red `×`; enforce the stability law that top-toolbar visible means no new canvas extent is manufactured, while hiding the top toolbar enables active drag/resize to create canvas space left/right/up/down with stable world coordinates; add explicit `New WEBX`, `New DOCX`, `New PDF`, `New Canvas`, and `Open WEBX…` paths; make Canvas a first-class drawing/editing object with honest SVG/PNG/JPG/WEBP export; preserve attached submenu geometry, PDF/DOCX/WEBX/Canvas native right-click menus, canonical Undo/Redo, WEBX Save/Open and web deployment readiness, and non-destructive `Convert → WEBX / DOCX / PDF`. Use up to 30 minutes, run full tests/syntax/diff/package validation, and at the 30-minute mark **conclude active implementation and provide a handoff**.