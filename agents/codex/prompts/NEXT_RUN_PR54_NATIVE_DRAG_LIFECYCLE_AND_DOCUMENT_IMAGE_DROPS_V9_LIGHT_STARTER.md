Read and execute `agents/codex/prompts/NEXT_RUN_PR54_NATIVE_DRAG_LIFECYCLE_AND_DOCUMENT_IMAGE_DROPS_V9.md` from the latest `main`. Treat it as the single authoritative continuation prompt for this run.

Continue the existing PR #54 branch `codex/fix-drag-ownership-model-for-image-insertion` from its latest remote head; do not start a competing implementation. The immediate blocker is the native HTML drag lifecycle: an internal FrameChute image drag must survive the browser's normal pointer-cancel transition after `dragstart`, keep its source Blob/session alive all the way to DOCX/PDF drop, and clean up at the correct end of the native drag. Re-verify all original outcomes as required by V9: no global `Drop into FrameChute` overlay while moving existing images, existing/OS images insert exactly once into DOCX, existing/OS images insert exactly once into PDF, originals remain, external workspace ingest still works, and PDF image edits save/reopen correctly.

Use the available runtime aggressively. At approximately the 30-minute reliability checkpoint, **conclude active implementation and provide a handoff** exactly as required by the authoritative prompt so a follow-up can immediately continue if needed.