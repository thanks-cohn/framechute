Read and execute `agents/codex/prompts/NEXT_RUN_POINT_CONTEXT_AND_FOUR_DIRECTION_CANVAS_V3.md` from the latest `main`. Treat it as the single authoritative prompt for this run and build on PR #51 rather than replacing its editor-context/menu foundations. Prioritize, in order: fix the remaining PR #51 invariants (Quick Actions red `×` always visible when the panel is visible, PDF Text Size mini-popover instead of browser `prompt()`, and no dead/orphan menu items); point-based right-click context that does not require preselection; true four-direction canvas expansion with stable world coordinates and no left/top clamp; top-toolbar `New → LightComp / DOCX / PDF`; and visible non-destructive `Convert → LightComp / DOCX / PDF` flows for PDF/DOCX through a shared semantic bridge. Preserve document-specific menus, Simple-mode restrictions, video-only sync capability, internal-drag ingest suppression, and preview/serialization correctness. Use up to 30 minutes, run the required tests/syntax/diff/package validation, and at the 30-minute mark **conclude active implementation and provide a handoff**.