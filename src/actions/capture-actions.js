const status=document.querySelector("#status");
const announce=(message)=>{if(status)status.textContent=message;};
const result=(blob,name,kind)=>window.dispatchEvent(new CustomEvent("framechute:add-result-object",{detail:{blob,name,kind}}));
async function displayStream(audio=false){if(!navigator.mediaDevices?.getDisplayMedia)throw new Error("Screen capture is unavailable.");return navigator.mediaDevices.getDisplayMedia({video:true,audio});}
export async function captureScreenImage(){const stream=await displayStream(),video=document.createElement("video");video.srcObject=stream;video.muted=true;await video.play();await new Promise(resolve=>requestAnimationFrame(()=>requestAnimationFrame(resolve)));const canvas=document.createElement("canvas");canvas.width=video.videoWidth;canvas.height=video.videoHeight;canvas.getContext("2d").drawImage(video,0,0);stream.getTracks().forEach(track=>track.stop());const blob=await new Promise(resolve=>canvas.toBlob(resolve,"image/png"));result(blob,`screenshot-${Date.now()}.png`,"image");return blob;}
export async function recordCapture({microphone=false}={}){let stream;if(microphone){if(!navigator.mediaDevices?.getUserMedia)throw new Error("Microphone capture is unavailable.");stream=await navigator.mediaDevices.getUserMedia({audio:true});}else stream=await displayStream(true);const types=[microphone&&"audio/webm;codecs=opus","video/webm;codecs=vp9,opus","video/webm;codecs=vp8,opus","video/webm"].filter(Boolean),mimeType=types.find(type=>MediaRecorder.isTypeSupported(type)),recorder=new MediaRecorder(stream,mimeType?{mimeType}:undefined),chunks=[];recorder.ondataavailable=event=>{if(event.data.size)chunks.push(event.data);};const stopped=new Promise(resolve=>recorder.onstop=resolve);recorder.start(1000);announce("Recording… click Stop recording when finished.");const stop=document.createElement("button");stop.type="button";stop.className="capture-stop";stop.textContent="Stop recording";document.body.append(stop);stop.onclick=()=>recorder.stop();stream.getTracks().forEach(track=>track.onended=()=>{if(recorder.state!=="inactive")recorder.stop();});await stopped;stop.remove();stream.getTracks().forEach(track=>track.stop());const type=recorder.mimeType||chunks[0]?.type||(microphone?"audio/webm":"video/webm"),blob=new Blob(chunks,{type}),kind=microphone?"audio":"video";result(blob,`${microphone?"microphone":"screen"}-${Date.now()}.webm`,kind);announce("Recording added to the workspace. Use Save As when ready.");return blob;}
const toolbar=document.querySelector("header .toolbar-primary, header");if(toolbar&&!document.querySelector("#capture-screenshot"))for(const [id,label,action] of [["capture-screenshot","Screenshot",()=>captureScreenImage()],["capture-screen-record","Record screen",()=>recordCapture()],["capture-microphone","Record mic",()=>recordCapture({microphone:true})]]){const button=document.createElement("button");button.id=id;button.type="button";button.textContent=label;button.title=`${label} (permission is requested only now)`;button.onclick=()=>action().catch(error=>announce(error.name==="NotAllowedError"?"Capture was cancelled or not allowed.":error.message));if(window.FrameChuteToolbarPager?.add)window.FrameChuteToolbarPager.add(button);else toolbar.append(button);}
